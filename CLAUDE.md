# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

Sprint Mission 12: JavaScript 정렬 알고리즘 구현. Node.js 부트캠프 (6기) 과제.

## 미션 요구사항

`algorithm/sorts.js`에 네 가지 정렬 알고리즘 구현:

- **선택 정렬** (`selectionSort`) — in-place, 입력 배열을 직접 수정
- **삽입 정렬** (`insertionSort`) — in-place, 입력 배열을 직접 수정
- **병합 정렬** (`mergeSort`) — 정렬된 새로운 배열을 리턴 (입력 배열 수정하지 않음)
- **퀵 정렬** (`quickSort`) — in-place, 입력 배열을 직접 수정

모든 함수는 숫자형 배열을 파라미터로 받음.

## 제출 안내

- 코드는 `algorithm/sorts.js`에 작성
- PR은 upstream의 `algorithm-{이름}` 브랜치를 대상으로 생성
